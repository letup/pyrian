#summary Development TODO list

  # States are getting too far apart in network games
  # Separate force event functionality from the event manager
  # Separate event logic from the object into a handler class